<?php
/**
 * Custom post type bootstrap include.
 *
 * The `lagoon` CPT is registered via {@see \Gin0115\Codelagoon\Features\PostType\LagoonPostType}
 * — see Plugin::initialize(). This file is intentionally empty; kept so the
 * mu-plugin's `glob('includes/*.php')` autoloader has a stable target if more
 * one-shot CPT bootstrap code is added later.
 *
 * @package Gin0115\Codelagoon\Features
 */

defined( 'ABSPATH' ) || exit;
